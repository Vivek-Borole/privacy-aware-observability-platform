name: synthetic compose integration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
      - run: bash scripts/integration-smoke.sh
      - if: always()
        run: docker compose logs --no-color
